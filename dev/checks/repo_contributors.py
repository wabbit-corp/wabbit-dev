"""
* [ ] Check that the contributors to repos all have valid email addresses. Check Git History
      for Generic Author Information: Scan commit history for commits authored using default
      or generic usernames/emails (e.g., root@localhost, admin@example.com, unconfigured Git
      defaults).
* [ ] Check that certain prohibited usernames/emails are not used in the commit history.
* [ ] Reference Issues and Tickets: If using an issue tracker, enforce that commit messages
      or PR descriptions reference relevant issue IDs (e.g., “Fixes #123”).
"""
